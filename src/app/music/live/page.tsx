import LiveRadioClient from "./LiveRadioClient";

export const metadata = {
    title: "Live Radio | Starlight",
    description: "Synchronized live radio experience.",
};

export default function LiveRadioPage() {
    return <LiveRadioClient />;
}
